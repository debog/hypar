````markdown
# Usage

Provide short examples for common tasks.

Example:

```python
from hypar import some_function

result = some_function(...)

print(result)
```

For more detailed usage, create example notebooks or code snippets and add them to this folder as separate pages or a dedicated `examples/` folder inside the docs.
````